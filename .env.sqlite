# Configuración para desarrollo local con SQLite
DATABASE_URL=sqlite:///./unxchange_auth.db

# Clave secreta para JWT (cambiar por una clave segura en producción)
SECRET_KEY=your-super-secret-key-change-this-in-production

# Configuración de tiempo de expiración del token (en minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Algoritmo para JWT
ALGORITHM=HS256